import parseJson from '../src/parse-json';

const generateJson = count => ({ signature_count: count });

describe('The `parseJson` method', () => {
  describe('when the JSON is valid', () => {
    it('test', () => {});
  });
});
